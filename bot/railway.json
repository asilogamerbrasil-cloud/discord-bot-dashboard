{
  "build": {
    "builder": "nixpacks",
    "config": {
      "installCommand": "cd .. && npm install && cd bot",
      "buildCommand": "npm run db:push",
      "startCommand": "npm run start:prod"
    }
  }
}
